apiVersion: v1
kind: Service
metadata:
  name: {{ include "bob-app.fullname" . }}
  labels:
    {{- include "bob-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bob-app.selectorLabels" . | nindent 4 }}
