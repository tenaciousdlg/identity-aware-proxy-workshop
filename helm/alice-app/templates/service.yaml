apiVersion: v1
kind: Service
metadata:
  name: {{ include "alice-app.fullname" . }}
  labels:
    {{- include "alice-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alice-app.selectorLabels" . | nindent 4 }}
